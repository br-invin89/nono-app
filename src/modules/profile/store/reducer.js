import * as types from './actionTypes';

const initialState = {
  cash: {
    money: 0
  }
}

export default function reducer(state = initialState, action) {
  switch(action.type) {
    default: 
      return state
  }
}
